fastapi==0.115.5
uvicorn[standard]==0.32.0
minio==7.2.10
python-multipart==0.0.12

# CPU-only torch — no CUDA, ~500 MB instead of ~2.5 GB
# Must be listed before openai-whisper so pip satisfies torch requirement from here
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
torchaudio==2.4.1+cpu

# openai-whisper picks up the already-installed CPU torch above
openai-whisper==20240930
